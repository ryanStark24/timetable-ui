{
  "name": "timetable-ui",
  "version": "0.1.0",
  "private": true,
  "homepage": "http://ryanstark24.github.io/timetable-ui",
  "dependencies": {
    "ajv": "^6.0.0",
    "ajv-keywords": "^1.1.0",
    "axios": "^0.18.0",
    "material-ui": "^0.20.0",
    "react": "^16.2.0",
    "react-bootstrap": "^0.32.1",
    "react-dom": "^16.2.0"
  },
  "devDependencies": {
    "gh-pages": "^1.1.0",
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "set PORT=3005 && react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -b gh-pages -d build"
  }
}
